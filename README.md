# Local Manifests

### Clone This Repository

```
git clone https://github.com/ehhhSenpai/local_manifests.git .repo/local_manifests
```

### Start Sync 

```
repo sync -j$(nproc --all) --no-tags --no-clone-bundle --current-branch
```
